#include <bits/stdc++.h>

using namespace std;

vector< int > v1, v2;

int main(){
    int t;

    scanf("%d", &t);

    while(t--){
        int n;

        scanf("%d", &n);

        for(int i = 0 ; i < n ; i++){
            scanf("%d", &a[i]);
        }
    }

    return 0;
}