#include <bits/stdc++.h>

using namespace std;

const int N = 50;
int x[N];

int main(){
    int t;

    scanf("%d", &t);

    while(t--){
        int n;

        scanf("%d", &n);

        for(int i = 0 ; i < n ; i++){
            scanf("%d", &x[i]);
        }

        
    }

    return 0;
}